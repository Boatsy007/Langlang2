# Resend API key — get yours at https://resend.com/api-keys
RESEND_API_KEY=re_...

# Sender address — must be a verified domain in your Resend dashboard.
# Until your domain is verified you can use: onboarding@resend.dev (test only)
RESEND_FROM_EMAIL=enquiries@langrestorations.com.au
